services:
  - type: web
    name: timemark-watermark
    env: node
    buildCommand: npm install --platform=linux --arch=x64 sharp && npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
    plan: free
